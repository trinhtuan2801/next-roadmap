import Default from '@/components/file-wrapper/Default';
import { Typography } from '@mui/joy';

export default function Slot2Default() {
  return (
    <Default name='slot-2'>
      <Typography level='body-sm'>default</Typography>
    </Default>
  );
}
